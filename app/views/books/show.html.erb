<h1> <%= @book.title %> </h1>
<h2> <%= @book.author %> <i>(<%= @book.genre.try(:name) %>)</i><small>      added by: <%= @book.user.name %></small></h2>
 <%= @book.description %><br><br>

 <% if current_user %>
 <strong><a href="/books/<%= @book.id %>/edit"> Edit</a>

 <%= link_to 'Edit', '/books/:id/edit' %>
 <%= link_to 'Delete',url_for(action: :delete,id: @book.id),method: :delete, data: {confirm: "Are you sure you want to delete this book?"} %>
